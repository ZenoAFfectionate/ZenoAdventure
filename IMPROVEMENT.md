# ZenoAdventure 系统审查与升级路线

> 审查日期：2026-07-21
> 审查基线：当前工作区代码、5 个关卡、4 名角色、持续扩展的自动化测试
> 文档目的：记录当前版本的真实状态，并按收益与风险给出后续升级顺序。

### 实施进度（2026-07-21）

| 项目 | 状态 |
|---|---|
| 目标时间与按关卡计算的时间奖励 | 已完成 |
| 统一踩踏、冲刺、星星击杀计分 | 已完成 |
| 无敌星与地形、掉落伤害规则 | 已完成 |
| 关卡统计、独立三星章与结算页 | 已完成 |
| 版本化 JSON 存档、损坏恢复、原子写入 | 已完成 |
| 关卡选择、解锁、最佳时间和历史星章 | 已完成 |
| Desert Dash 左右传送带 | 已完成 |
| Crystal Cave 水池、鱼与水下宝石 | 已完成 |
| 火焰史莱姆踩踏后火焰区域 | 已完成 |
| Green Hills 角色技能教学、星星规则提示与教程开关 | 已完成 |
| Desert Dash 传送带节奏教学组合 | 已完成 |
| Crystal Cave 浅水→深水渐进教学与出水暗礁 | 已完成 |
| Lava Lake 终段弹簧飞越与 Purple 减速延长火焰预警 | 已完成 |
| Sky Temple 底层恢复岛、垂直移动平台与上层锁门 | 已完成 |
| 敌人身份强化（踢壳、蜂蝇分化、轨道锯齿、纵向鱼、青蛙预警、尖刺可破） | 已完成 |
| 方块守门者三阶段首领与首领前检查点 | 已完成 |
| 检查点间距均衡（L1/L2/L4 补点） | 已完成 |
| 难度、成就、手柄和设置界面 | 待实施 |

---

## 一、结论摘要

当前版本已经具备一款完整小型横版平台游戏的基本条件：操作手感稳定，五关可以完整通关，角色技能有明显差异，碰撞、检查点、钥匙门、收集品、敌人和多种机关都能正常运行。现有测试对地图可达性、边界、技能和基础状态机的覆盖也比较扎实。

下一阶段不应单纯继续增加地图长度或敌人数量，而应优先解决以下问题：

1. **终关存在绕过核心挑战的可能**：Sky Temple 底部有连续安全地面，玩家掉下空中路线后可以沿底层前进，移动平台的终局价值被削弱。
2. **部分敌人仍可继续深化**：火焰史莱姆已经具备火焰区域，锯齿仍是静态碰撞体，没有位置或节奏变化。
3. **角色收益仍有一处不对齐**：Green 冲刺击杀已纳入统一奖励，但 Beige 作为挑战角色仍没有额外结算标识或奖励。
4. **长期目标仍可扩展**：关卡选择、存档和三星章已经完成，难度、固定挑战任务、成就和终关首领仍未实现。
5. **可访问性不足**：缺少手柄、按键重绑定、独立音量、屏幕震动开关和语言资源。

推荐顺序：先完成规则闭环和结算评价，再强化关卡路线，之后增加敌人与首领，最后处理设置、音频和工程重构。

---

## 二、当前版本盘点

### 2.1 已经完成且应保留的优势

| 领域 | 当前状态 | 评价 |
|---|---|---|
| 移动手感 | 可变跳跃、土狼时间、跳跃缓冲、空中控制 | 已达到可继续扩展的基础质量 |
| 角色 | Beige、Green、Pink、Purple 四种玩法 | 技能辨识度良好 |
| 战斗 | 踩踏、蜗牛两阶段、连击、击退、无敌帧 | 核心闭环已存在 |
| 机关 | 弹簧、梯子、传送带、移动平台、尖刺、熔岩 | 类型丰富，但关卡利用率不均衡 |
| 探索 | 高低路线、技能水晶、宝石、星星、心 | 有风险奖励雏形 |
| 流程 | 菜单、选人、暂停、关卡结算、失败、胜利 | 主流程完整 |
| 稳定性 | 左右边界双重保护、检查点、地图自动验证 | 基础可靠 |
| 测试 | 150 项测试，含五关自动通关 | 回归能力较好 |

### 2.2 五关实际内容

| 关卡 | 主要机制 | 当前亮点 | 主要不足 |
|---|---|---|---|
| Green Hills | 基础跳跃、弹簧、踩踏、钥匙门 | 教学顺序清楚，高处支线容易理解 | 教程只覆盖基础操作，没有介绍角色技能和无敌星 |
| Desert Dash | 传送带、飞行敌人、尖刺、双检查点 | 高低路线和恢复点安排合理 | 只有向右传送带，机关变化有限；飞行敌人集中在高处，对主路压力偏弱 |
| Crystal Cave | 梯子、尖刺、锯齿、水池、鱼、垂直奖励 | 主桥安全，水下支线提供额外奖励 | 梯子主要通向奖励，仍可强化上下层路线选择 |
| Lava Lake | 连续熔岩坑、弹簧、火焰区域、混合敌人 | 当前难度和主题结合最好 | 后半段可增加节奏变化而不是继续加坑 |
| Sky Temple | 九个移动平台、空中路线、综合敌人 | 具备终关规模和视觉辨识度 | 连续底层地面可以弱化空中挑战；缺少终关首领或独有终局机制 |

### 2.3 已实现但没有充分使用的内容

- `W` 水域、`f` 鱼和水下宝石已经在 Crystal Cave 支线使用。
- `<` 与 `>` 双向传送带已经在 Desert Dash 使用。
- `time_limit` 已作为 Normal 模式目标时间接入 HUD、奖励和技巧星章。
- `transition_alpha` 与 `transition_dir` 已定义，但没有形成统一的场景过渡状态机。
- 素材包仍包含开关、拉杆、箱子、炸弹、火球、彩色钥匙和更多敌人，尚未形成游戏规则。

这些内容不应全部一次性加入。每增加一种机制，至少需要一个安全教学段、一个组合应用段和对应测试。

---

## 三、最高优先级：完善核心规则闭环

### 3.1 明确并启用关卡时间规则

**状态：已完成**

`time_limit` 已定义为 Normal 模式的目标时间，而不是强制死亡倒计时。HUD 同时显示已用时间和目标时间，奖励按各关目标时间比例计算。

- HUD 显示 `Elapsed / Target`。
- 在目标时间内通关获得时间星章。
- 超时后仍可继续，但失去该星章。
- Hard 模式再将其改为真正倒计时。
- 时间奖励按各关 `time_limit` 比例计算，不再使用统一的 100 秒隐含阈值。

**验收标准**

- 五关的目标时间均被结算逻辑读取。
- 目标时间前后完成产生不同评价，但 Normal 模式不会因超时直接失败。
- 暂停期间不累计时间。
- 对应单元测试覆盖目标时间边界。

### 3.2 建立关卡结算评价

**状态：已完成**

结算页已经统计收集品、击杀、死亡和时间，并提供三个独立星章：

1. **通关星**：到达出口。
2. **探索星**：收集率达到 100%。
3. **技巧星**：在目标时间内且本关未死亡。

结算页应显示：

- 本关时间与最佳时间。
- 收集数量与总数量。
- 击败敌人数与总数。
- 本关死亡次数。
- 获得的星章与历史最佳星章。

这种评价比简单按总分划分一至三星更清晰，因为玩家能够知道下一次重玩的具体目标。

### 3.3 修正技能击杀的奖励一致性

**状态：已完成**

- 踩踏敌人会增加分数、连击、粒子和浮动文字。
- Green 冲刺、踩踏和无敌星击杀已经经过统一入口记录分数、击杀数与反馈。

**建议**

统一所有击杀入口：

```text
Enemy defeated
  -> 记录击杀方式
  -> 计算基础分
  -> 更新连击或技能连杀
  -> 播放音效与粒子
  -> 更新关卡统计
```

- 踩踏保留空中连击。
- 冲刺使用独立的连续命中奖励，例如同一次冲刺第 1/2/3 个敌人分别为 30/60/90 分。
- 无敌星击杀计入总击杀，但不应无限刷新踩踏连击。
- 尖刺史莱姆和锯齿是否能被冲刺消灭必须明确；建议锯齿不可消灭，尖刺史莱姆可被冲刺消灭。

### 3.4 明确无敌星规则

**状态：已完成**

无敌星现在可以抵消一次尖刺、熔岩或临时火焰伤害，随后立即结束；掉出地图会忽略星星保护。

推荐规则：

- 防御普通敌人和可踩踏敌人，并允许接触击杀。
- 不防御掉出地图。
- 熔岩和尖刺可以防御一次，但立即结束星星状态并弹开玩家。
- HUD 在最后 1 秒快速闪烁，并播放结束提示音。

这比“完全免疫所有危险”更能保留关卡挑战。

---

## 四、关卡与机关升级

### 4.1 Green Hills：补充技能与探索教学

**状态：已完成**

- 第一个技能水晶前按当前角色显示对应技能提示；Beige 显示 `Classic Challenge`。
- 高处支线末端增加金币下落轨迹，清晰指示落回主路的位置。
- 首次取得无敌星时展示短暂规则提示（全局一次）。
- 教程提示每条只出现一次，并可在 Controls 界面按 `T` 关闭（持久化到存档）。

建议保持低难度，不增加新敌人数量。

- 在第一个技能水晶前，根据当前角色显示对应技能提示。
- 在无技能的 Beige 模式中，将提示替换为“Classic Challenge”。
- 高处支线末端增加清晰的落回主路位置，避免玩家误以为必须原路返回。
- 第一次取得星星时展示短暂的规则提示。
- 教程提示只出现一次，并允许在设置中关闭。

### 4.2 Desert Dash：让传送带成为真正的节奏机关

**状态：已完成**

 cols 32-43 已实现教学组合「安全地面 → 右向带 → 小坑 → 左向带 → 单个尖刺 → 安全区」，首次出现的左向带后连接安全地面；玩家逆向移动仍可抵消传送带；高处路线保留飞行敌人作为更快但更危险的捷径。

建议增加一个短小的教学组合：

```text
安全地面 -> 右向带 -> 小坑 -> 左向带 -> 单个尖刺 -> 安全区
```

设计约束：

- 首次出现的左向带后不能直接连接死亡坑。
- 玩家逆向移动仍能抵消传送带，不应失去控制权。
- Hard 模式可以提高传送带速度，而不是增加更多尖刺。
- 高处路线保留飞行敌人，作为更快但更危险的捷径。

### 4.3 Crystal Cave：加入水域与鱼的完整教学段

**状态：已完成**

这是利用现有代码和素材获得最大内容收益的位置。

Crystal Cave 已加入带安全桥面的水池支线，并补充渐进式教学与保底出路：

- 主路从水池上方桥面通过，可以正常通关。
- 水下路线包含蓝宝石和一条严格限制在水域内的鱼。
- 入口先用浅水展示减速效果，再进入深水：cols 43-44 为及腰浅水台，cols 45-48 为深水池。
- 池尾 row 9 暗礁（单向板）与出口地面齐平，保证不依赖踩鱼也有出路。
- 明确水中是否存在氧气规则：第一版不加入溺水计时。
- 鱼只在水域范围内巡逻，不能穿过地面或游出水池。

验收时需要增加：水中跳跃/下落速度、离水恢复、鱼活动边界和支线奖励可达性测试。

### 4.4 Lava Lake：强化敌人差异而非增加更多坑

**状态：已完成**

火焰史莱姆被踩踏后会生成带预警的短暂火焰；冲刺击杀不会生成火焰。Purple 的减速会延长火焰预警时间（与敌人减速同步，1/4 速率），但不延长火焰持续时间。后半段改为弹簧飞越最终熔岩坑的节奏变化（金币弧线奖励 + 落点青蛙），而非继续加坑。

- 被踩踏后在落点留下约 1.5 秒火焰。
- 火焰出现前有 0.3 秒闪烁预警。
- 火焰不能与熔岩坑连续放置，必须留有安全落点。
- Green 可以冲刺消灭它而不生成火焰。
- Purple 减速同时延长预警时间，但不延长火焰持续时间。
- Pink 可以用二段跳越过火焰区域。

这样三个技能角色都能以不同方式应对同一个敌人。

### 4.5 Sky Temple：防止绕过移动平台主挑战

**状态：已完成**

已采用“可恢复但不能绕关”方案重构：

- 底层拆成 4 个安全岛（cols 0-13 / 20-35 / 42-56 / 64-78），中间以 6 格以上虚空分隔，持续向右无法到达出口。
- 每座岛放置 2 个弹簧（中段 + 右端），弹回空中路线，不形成软锁。
- 锁（95,7）与出口（115,7）保留在上层路线，底层仅用于失误恢复。
- 中后段加入 2 个垂直移动平台（`V`，首个低速 0.8 px/帧），并支持玩家站立随平台平稳升降。
- 最后 cols 98-119 为综合挑战（移动平台 + 尖刺史莱姆 + 青蛙 + 蜜蜂 + 收集 + 出口），不引入新规则。

验收（均有自动化测试覆盖）：不使用技能仍可通关；底层持续向右不能到达出口；每个安全岛都能返回主路线；移动平台扫掠路径无实体阻挡且不越界。

- 将底层拆成 3 至 4 个安全岛，中间使用虚空或尖刺分隔。
- 安全岛放置返回上层的弹簧或单向移动平台。
- 锁和出口保留在上层，底层只能用于失误恢复。
- 中后段移动平台增加垂直移动变体，但首次出现时保持低速。
- 最后 15 格设置一个只使用前面已教学机制的综合挑战，不再引入新规则。

**验收标准**

- 不使用角色技能仍可通关。
- 单纯掉到底层并持续向右不能到达出口。
- 每个底层安全岛都能返回主路线，不形成软锁。
- 移动平台不会把玩家推入墙体或地图边界。

### 4.6 机关组合原则

每个新机关遵循三步结构：

1. **展示**：安全环境中单独出现。
2. **练习**：与一个旧机制组合。
3. **考核**：在检查点后形成短挑战。

避免同时组合三种以上高惩罚元素。例如“移动平台 + 飞行敌人 + 尖刺 + 掉落即死”不应首次一起出现。

---

## 五、敌人与战斗升级

### 5.1 完善现有敌人身份

**状态：已完成**

| 敌人 | 当前作用 | 实施结果 |
|---|---|---|
| 普通史莱姆 | 基础巡逻 | 保持教学敌人，未增强 |
| 蜗牛 | 两次踩踏 | 缩壳后可被踢动：限速 6 px/帧，撞倒首个敌人后停下（连锁限一次） |
| 蜜蜂/苍蝇 | 正弦飞行 | 蜜蜂宽幅横向低幅巡逻，苍蝇窄幅纵向高幅俯冲 |
| 锯齿 | 静态不可踩踏 | 新增 `Z` 轨道锯齿变体：正弦往返，轨道线 + 端点预览，不可消灭 |
| 火焰史莱姆 | 踩踏后生成短暂火焰 | 火焰末段闪烁收缩熄灭，并伴随烟雾粒子 |
| 尖刺史莱姆 | 不可踩踏 | 可被冲刺或无敌星消灭，+100 与 `ARMOR BREAK!` 明确反馈 |
| 青蛙 | 周期跳跃 | 起跳前压缩下蹲并显示逐渐增强的落点阴影 |
| 鱼 | 洞穴水池内巡逻 | 新增 `w` 纵向紫鱼变体，严格限制在水柱内上下游动 |

### 5.2 增加小型首领，而不是普通敌人堆叠

**状态：已完成**

终关加入方块守门者（复用素材包 block 系列），三阶段循环：

1. **落点预警**（0.75 秒）：地面出现逐渐增大的红色落点标记与下落指示柱。
2. **天降砸落**：落地震屏，随后 0.25 秒蓄力闪烁，产生双向可跳过地面冲击波。
3. **暴露弱点**（2.5 秒）：玩家踩踏一次。

循环三次后解除出口门封印。首领只在玩家落地进入竞技场后激活；坠落与冲击波是仅有的伤害来源（虚弱期接触无害），不同角色技能只能提供优势（Purple 减速同步拉长首领所有计时），不能跳过战斗。

首领验收（均有自动化测试覆盖）：

- 所有角色均可无伤击败（公平规则 + 全角色通关模拟）。
- 攻击前有不少于 0.5 秒可读预警（0.75 秒落点预警）。
- 失败后从首领前检查点（col 97 旗帜）重生，首领状态完全重置。
- 首领状态在暂停时完全停止。
- 自动测试覆盖阶段切换、踩踏开门、暂停冻结、减速拉长与重生重置。

---

## 六、角色平衡与重玩价值

### 6.1 为 Beige 提供明确定位

Beige 目前是 3 HP、无技能的挑战角色，但回报与其他角色相同。

建议：

- 选择界面标记为 `Classic Challenge`。
- 使用 Beige 通关获得 20% 分数加成或独立徽章。
- 不要通过增加移动速度补偿，否则会削弱其“基础规则挑战”定位。

### 6.2 避免角色专属硬门槛

- 所有主线必须能由 Beige 完成。
- Pink 可以更容易拿到高处奖励，但奖励仍应能通过弹簧或精确跳跃获得。
- Green 可以快速处理敌群，但不能穿过锁、墙或首领阶段。
- Purple 的减速应影响敌人与活动机关的危险节奏，但不应减慢关卡计时。

### 6.3 增加每日或轮换挑战前，先做固定挑战

第一版挑战任务建议固定且可测试：

- 本关无伤。
- 收集全部宝石。
- 不使用技能通关。
- 在目标时间内通关。
- 连续踩踏 3 个敌人。

完成后只奖励徽章、分数倍率或角色外观，不要提供影响基础数值的永久强化，避免破坏关卡平衡。

---

## 七、进度、难度与长期目标

### 7.1 关卡选择与进度存档

新增 `save.json`，至少保存：

- 已解锁最高关卡。
- 每关最佳时间、最高分、最高收集率和星章。
- 各角色的通关记录。
- 音量、屏幕震动、教程提示等设置。

存档要求：

- 使用临时文件写入后原子替换，避免中途退出损坏。
- 文件损坏时回退默认值并备份旧文件。
- 保存数据带版本号，便于未来迁移。
- `save.json` 加入 `.gitignore`。

### 7.2 三档难度

建议难度只改变少量、可解释的参数：

| 参数 | Easy | Normal | Hard |
|---|---:|---:|---:|
| 初始生命 | 5 | 3 | 2 |
| 敌人速度 | 85% | 100% | 115% |
| 检查点 | 全部 | 全部 | 每关仅主要检查点 |
| 时间 | 仅目标 | 仅目标 | 倒计时 |
| 地形布局 | 不变 | 不变 | 少量可选危险启用 |

不要为三档难度维护三套完全不同的地图，这会显著增加测试和维护成本。

### 7.3 成就系统

优先选择能引导不同玩法的成就，而不是单纯累计次数：

- 第一次完成空中三连踩踏。
- 用每名角色完成任意关卡。
- 使用 Beige 通关最终关。
- 五关全部获得探索星。
- 在不拾取心的情况下完成 Lava Lake。
- 从 Sky Temple 的每个底层恢复岛返回主路线。

成就弹窗不能遮挡玩家或暂停游戏，结算页再显示详细说明。

---

## 八、视听反馈与可访问性

### 8.1 音频

当前只有音效，没有背景音乐和独立音量控制。

建议：

- 菜单、普通关、洞穴/熔岩、终关各使用一组循环音乐。
- 音乐音量和音效音量分开设置。
- 失去焦点时自动降低或暂停音频。
- 移动平台和传送带不必持续播放高频循环音，避免听觉疲劳。

### 8.2 视觉可读性

- 危险地形使用一致轮廓和颜色语义。
- 青蛙、落石、首领攻击增加落点预警。
- 无敌星最后一秒闪烁，但提供“减少闪烁”选项。
- Purple 减速不要使用覆盖全屏的高不透明度颜色。
- 检查点激活后更换旗帜颜色，不能只依赖音效。

### 8.3 控制与辅助选项

- 支持手柄，并显示当前输入设备对应图标。
- 支持按键重绑定。
- 增加屏幕震动开关和强度选项。
- 增加教程提示开关。
- 提供窗口/全屏切换与整数倍缩放。
- 中英文文本从代码中抽离，为本地化做准备。

---

## 九、技术与测试改进

### 9.1 拆分职责过重的模块

`game.py` 目前同时承担地图解析、渲染、菜单、流程、碰撞交互和 HUD；`entities.py` 也同时包含粒子、收集品、敌人和玩家。

建议在功能继续增加前逐步拆分：

```text
src/
  game.py          主循环与状态切换
  tilemap.py       地图解析、碰撞查询、地图渲染
  player.py        玩家状态与技能
  enemies.py       敌人基类和各类 AI
  ui.py            菜单、HUD、结算页
  progression.py   存档、星章、关卡解锁
```

拆分必须保持行为不变，并在每一步后运行全部测试，不能与大规模玩法改动同时进行。

### 9.2 使用统一事件接口

踩踏、冲刺和无敌星击杀已经统一进入 `_on_enemy_defeated()`。后续可以继续把收集、伤害和检查点也收敛为轻量事件接口：

- `on_collect(item)`
- `on_enemy_defeated(enemy, method)`
- `on_player_damaged(source)`
- `on_checkpoint_activated(checkpoint)`
- `on_level_completed(stats)`

不需要引入复杂事件总线，只要让相同行为经过同一处理函数。

### 9.3 固定帧逻辑与真实时间

主循环计算了 `dt`，但绝大多数物理仍按每帧固定数值运行。当前锁定 60 FPS 时可以工作，但卡顿或未来调整刷新率会改变体验。

短期建议维持固定 60 Hz 更新、渲染与更新解耦；长期再评估把速度统一为每秒单位。不要直接把现有常量乘 `dt`，这会改变所有跳跃曲线。

### 9.4 存档与错误处理

- 最高分不应继续使用单独的纯文本文件，应迁移到版本化 JSON 存档。
- 背景绘制中的宽泛 `except Exception` 会隐藏素材名称错误，应只捕获可预期异常并记录日志。
- 增加 `pyproject.toml` 或 `requirements.txt`，明确 Python 和 Pygame 版本。
- 提供一条统一验证命令，例如 `python -m pytest`。

### 9.5 测试缺口

现有测试较强，已补充 `tests/simulate.py` 游玩模拟框架：以脚本化输入驱动真实 `_update_playing()` 主循环完成通关，替代手写的碰撞流程复制；`tests/test_playtest.py` 覆盖全角色×全关卡完成矩阵、战役通关、首领封印、检查点重生、提示触发与目标时间余量。仍应补充：

- ~~使用真实 `_update_playing()` 的完整关卡流程测试~~（已完成）。
- 存档创建、升级、损坏恢复和原子写入测试。
- 三档难度参数与关卡可达性测试。
- 水域、鱼、左向传送带和火焰区域测试。
- 移动平台长时间承载、反向和边界测试。
- 暂停时计时器、敌人、技能冷却和首领完全停止的测试（首领暂停已覆盖）。
- 多分辨率 UI 截图检查。
- 30 分钟无窗口运行的资源与性能稳定性测试。

---

## 十、推荐实施路线

### Phase 0：规则一致性

**状态：已完成。**

- 启用目标时间和基于关卡的时间奖励。
- 统一所有敌人击杀和分数入口。
- 明确无敌星与地形伤害规则。
- 修正 README 中鱼和水域的实际状态，或同步加入对应关卡。
- 增加关卡统计对象：收集、击杀、死亡、技能使用、时间。

**完成定义**：规则在 HUD、结算、README、代码和测试中一致。

### Phase 1：重玩循环

**状态：主体已完成，Beige 挑战徽章待实施。**

- 关卡结算三星章。
- 关卡选择和解锁。
- 最佳时间、最高收集率和角色通关记录。
- 版本化 JSON 存档。
- Beige 挑战徽章。

**完成定义**：玩家通关一次后，能够明确选择下一项可追求目标。

### Phase 2：现有关卡深挖

**状态：关卡深挖全部完成；固定挑战任务待实施。**

- Desert Dash 加入左右传送带组合。
- Crystal Cave 加入水域和鱼支线。
- Lava Lake 完成火焰史莱姆独特行为。
- Sky Temple 拆分底层安全地面，防止绕过空中路线。
- 为每关增加一项固定挑战任务（待实施）。

**完成定义**：所有已实现机关至少在一关中完成“展示、练习、考核”。

### Phase 3：终局内容

**状态：首领已完成；其余待实施。**

- 方块守门者小型首领（已完成）。
- 终关专属结算和音乐（待实施）。
- Hard 模式（待实施）。
- 成就系统（待实施）。

**完成定义**：最终关提供区别于普通关卡的高潮，而不只是更长的平台序列。

### Phase 4：打磨与发布

- 手柄、重绑定、音量和辅助设置。
- 场景淡入淡出和检查点视觉状态。
- 模块拆分、依赖声明和打包脚本。
- 多分辨率截图、长时间运行和性能验证。

**完成定义**：新玩家无需查看源码或 README 即可完成设置、理解目标并通关。

---

## 十一、建议暂缓的内容

以下功能开发成本高，但对当前核心体验提升有限，建议在 Phase 3 之前暂缓：

- 随机关卡生成。
- 在线排行榜或多人模式。
- 大型装备、等级或技能树。
- 每个角色完全独立的专属关卡。
- 大量一次性新敌人。
- 复杂剧情对话系统。

当前最有价值的方向是让五关、四名角色和已有机关产生更多有意义的组合与重玩目标，而不是把项目规模迅速扩大。

---

## 十二、版本升级验收清单

每个 Phase 完成后至少满足：

- [ ] Beige、Green、Pink、Purple 均可完成全部主线。
- [ ] 所有钥匙、锁、检查点、出口和必经机关无软锁。
- [ ] 主路线不依赖特定角色技能。
- [ ] 可选路线的奖励与风险成比例。
- [ ] 玩家受伤前有可读的危险提示。
- [ ] 关卡出口前不存在无法游玩的剩余内容。
- [ ] HUD、结算页、README 与实际规则一致。
- [ ] 全部自动化测试通过。
- [ ] 四名角色完成多随机种子通关模拟。
- [ ] 菜单、角色选择、暂停、结算、失败和胜利页面完成截图检查。
- [ ] 游戏主循环能够正常启动并干净退出。
- [ ] 没有新增未跟踪的运行缓存或存档文件。

完成上述闭环后，ZenoAdventure 将从“内容完整的短篇平台游戏”提升为“有明确挑战目标、角色重玩价值和稳定进度系统的完整作品”。
